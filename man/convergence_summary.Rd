% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ConvergenceDiagnostics.R
\name{convergence_summary}
\alias{convergence_summary}
\title{Full convergence summary for a gibbs_postscm fit}
\usage{
convergence_summary(
  fit,
  gdata = NULL,
  sampler_fn = NULL,
  package = NULL,
  run_rhat = FALSE,
  n_chains = 4L,
  n_iter = 1000L,
  burn_in = 500L,
  control = NULL,
  min_ess = 200,
  rhat_thresh = 1.1,
  show_traces = TRUE
)
}
\arguments{
\item{fit}{Output from gibbs_postscm(). Required.}

\item{gdata}{Prepared data object. Required only if run_rhat = TRUE.}

\item{sampler_fn}{The sampler function. Passed to compute_rhat(). Defaults
to gibbs_postscm if found, otherwise supply explicitly.}

\item{run_rhat}{Logical. Run multi-chain R-hat? Default FALSE.}

\item{n_chains}{Passed to compute_rhat(). Default 4.}

\item{n_iter}{Passed to compute_rhat(). Default 1000.}

\item{burn_in}{Passed to compute_rhat(). Default 500.}

\item{control}{Passed to compute_rhat().}

\item{min_ess}{ESS adequacy threshold. Default 200.}

\item{rhat_thresh}{R-hat convergence threshold. Default 1.1.}

\item{show_traces}{Logical. Display trace plots? Default TRUE.}
}
\value{
Invisibly returns a list with elements:
$ess               data frame from compute_ess()
$posterior_summary data frame of means and 95\% credible intervals
$rhat              data frame from compute_rhat() (only if run_rhat = TRUE)
}
\description{
Combines trace plots, ESS, posterior summaries, and optionally Gelman-Rubin
R-hat into a single printed report. Mirrors the convergence assessment
described in the paper.
}
