% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ConvergenceDiagnostics.R
\name{compute_rhat}
\alias{compute_rhat}
\title{Gelman-Rubin R-hat from multiple independent chains}
\usage{
compute_rhat(
  gdata,
  sampler_fn = NULL,
  package = NULL,
  n_chains = 4L,
  n_iter = 1000L,
  burn_in = 500L,
  control = NULL,
  seeds = NULL,
  threshold = 1.1
)
}
\arguments{
\item{gdata}{Prepared data object passed to the sampler.}

\item{sampler_fn}{Function to use as the sampler. Defaults to
gibbs_postscm if it can be found, otherwise must be supplied
explicitly, e.g. sampler_fn = gibbs_postscm.}

\item{package}{Character. Name of the package to load inside each worker
so that sampler dependencies are available. E.g. package = "scmBayesPost".}

\item{n_chains}{Integer. Number of independent chains. Default 4.}

\item{n_iter}{Integer. Total iterations per chain.}

\item{burn_in}{Integer. Burn-in iterations per chain.}

\item{control}{Optional control list passed to the sampler.}

\item{seeds}{Integer vector of length n_chains for reproducibility.
Randomly generated if NULL.}

\item{threshold}{Scalar R-hat threshold for flagging. Default 1.1.}
}
\value{
A data frame with columns: parameter, rhat, converged.
The list of fitted chains is attached as attr(result, "fits") and
the seeds used as attr(result, "seeds").
}
\description{
Re-runs the sampler n_chains times from different random seeds and computes
R-hat for all monitored parameters. Chains run in parallel via
parallel::mclapply on Mac/Linux, sequentially on Windows.
}
