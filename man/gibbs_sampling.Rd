% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gibbs_sampling.R
\name{gibbs_sampling}
\alias{gibbs_sampling}
\title{Gibbs sampler for post–synthetic-control hierarchical models (internal)}
\usage{
gibbs_sampling(
  gdata,
  n_iter = 1000,
  burn_in = 500,
  run_selection_gibbs = FALSE,
  do.CF = TRUE,
  selection.method = c("2SRI", "GR"),
  Z_cov_dense = FALSE,
  CF.interactions = TRUE
)
}
\arguments{
\item{gdata}{A data object produced by \code{\link[=prepare_data_general]{prepare_data_general()}}. Must contain
at least \code{Y_block}, \code{X_block}, and \code{W}. For second-stage models it must also
contain \code{Z_block} and, if IV functionality is used, \code{Z.instruments}.}

\item{n_iter}{Integer; total number of Gibbs iterations.}

\item{burn_in}{Integer; number of initial iterations discarded as burn-in.}

\item{run_selection_gibbs}{Logical; if \code{TRUE}, include a Gibbs block for a
binary treatment/selection equation and update the relevant column of
\code{X_block} with the resulting residual (2SRI) or generalized residual (GR).
Requires \code{gdata$first_stage} components (e.g., \code{GRX}, \code{GRY}) and a column in
\code{X_block} corresponding to the residual (typically \code{"GR"}).}

\item{do.CF}{Logical; if \code{TRUE}, apply a control-function approach when
\code{gdata$Z.instruments} is provided (append first-stage residuals, optionally
interacted). If \code{FALSE}, use a 2SLS-style replacement with first-stage
predicted values. Ignored when no instruments are provided.}

\item{selection.method}{Character; residual type used in the selection block:
\code{"2SRI"} uses probit residual inclusion; \code{"GR"} uses generalized residuals.}

\item{Z_cov_dense}{Logical; if \code{TRUE}, estimate a dense covariance matrix for
the first-stage IV residuals (requires \code{MCMCpack} for inverse-Wishart draws);
if \code{FALSE}, assumes a diagonal covariance.}

\item{CF.interactions}{Logical; if \code{TRUE} and \code{do.CF = TRUE}, include
interaction terms among control-function residuals.}
}
\value{
A list of posterior draws. Components include (depending on model):
\describe{
\item{beta_samples}{Matrix of draws for stacked second-stage coefficients.}
\item{gamma_samples}{Matrix of draws for moderator effects.}
\item{sigma2_samples}{Vector of draws for outcome noise variance.}
\item{tau_samples}{Matrix of draws for hierarchical scale parameters.}
\item{beta_fs_samples}{(Optional) draws for selection-equation coefficients.}
\item{sigma2_fs_samples}{(Optional) draws for selection-equation variance.}
\item{beta.Z_samples}{(Optional) list of draws for IV first-stage coefficients.}
\item{sigma.Z_samples}{(Optional) draws for IV covariance (diagonal or dense).}
}
}
\description{
Runs the full Gibbs sampler for the second-stage Bayesian model used by
\code{scmBayesPost} when moderator and/or endogeneity components are present.
This function is \strong{internal} and is called by \code{\link[=gibbs_postscm]{gibbs_postscm()}}.
}
\details{
The sampler targets a hierarchical regression of treated-unit (or unit–event)
treatment-effect parameters on moderator covariates, with optional
endogeneity correction via (i) a selection-equation Gibbs block for binary
treatment assignment, and/or (ii) instrumental-variables/control-function
blocks for endogenous moderators.
}
\seealso{
\code{\link[=gibbs_postscm]{gibbs_postscm()}}, \code{\link[=prepare_data_general]{prepare_data_general()}}
}
\keyword{internal}
