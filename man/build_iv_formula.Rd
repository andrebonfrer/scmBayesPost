% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_iv_formula.R
\name{build_iv_formula}
\alias{build_iv_formula}
\title{Build IV formula for use with prepare_data_general}
\usage{
build_iv_formula(
  outcome,
  treatment = "budgetdummy",
  instruments = NULL,
  controls = NULL,
  intercept = FALSE
)
}
\arguments{
\item{outcome}{Character. Outcome variable name.}

\item{treatment}{Character. Treatment variable name.
Default \code{"budgetdummy"}.}

\item{instruments}{Character vector. Instrument variable names.
Default \code{NULL}.}

\item{controls}{Character vector. Control variable names to include
in the instrument equation only (not the outcome equation).
Default \code{NULL}.}

\item{intercept}{Logical. Include intercept in outcome equation.
Default \code{FALSE} (recommended for SCM — weights absorb the level).}
}
\value{
A formula with attributes \code{treatment}, \code{instruments},
and \code{controls}.
}
\description{
Constructs a two-block formula of the form
\code{outcome ~ 0 + treatment | treatment ~ instruments + controls}
for use with \code{prepare_data_general()} and
\code{instrument_validity_report()}. Instrument and control variable
names are stored as formula attributes for reliable downstream parsing
by \code{.parse_iv_formula()}.
}
